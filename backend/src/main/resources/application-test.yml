spring:
  datasource:
    url: jdbc:h2:mem:invitation;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: false

app:
  jwt:
    secret: test-only-secret-with-at-least-32-characters
    expiration-seconds: 3600
    issuer: https://test.invitation.local
